import {motion} from 'framer-motion';import {ArrowUpRight} from 'lucide-react';import {Link} from 'react-router-dom';import './CourseCard.css';
export default function CourseCard({course,index=0}){const Icon=course.icon;return <motion.article className="course-card" initial={{opacity:0,y:25}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:index*.1}} whileHover={{y:-8}}><div className="course-image"><img src={course.image} alt={course.title}/><span><Icon size={18}/></span></div><div className="course-body"><small>ONLINE COURSE</small><h3>{course.title}</h3><p>{course.description}</p><Link to="/courses">Learn More <ArrowUpRight size={16}/></Link></div></motion.article>}
