import {motion} from 'framer-motion';import {useLanguage} from '../../context/LanguageContext';import './FeatureCard.css';
export default function FeatureCard({icon:Icon,title,children,index=0}){const {t}=useLanguage();return <motion.article className="feature-card" initial={{opacity:0,y:22}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.2}} transition={{delay:index*.07}} whileHover={{y:-7,rotateX:2,rotateY:-2}}><div className="feature-icon"><Icon size={21}/></div><h3>{t(title)}</h3><p>{t(children)}</p></motion.article>}
