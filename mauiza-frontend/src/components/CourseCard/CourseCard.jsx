import {motion} from 'framer-motion';import {ArrowUpRight} from 'lucide-react';import {Link} from 'react-router-dom';import {useLanguage} from '../../context/LanguageContext';import './CourseCard.css';
export default function CourseCard({course,index=0}){const {t}=useLanguage();const Icon=course.icon;return <motion.article className="course-card" initial={{opacity:0,y:25}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:index*.1}} whileHover={{y:-8}}><div className="course-image"><img src={course.image} alt={t(course.title)}/><span><Icon size={18}/></span></div><div className="course-body"><small>{t("ONLINE COURSE")}</small><h3>{t(course.title)}</h3><p>{t(course.description)}</p><Link to="/courses">{t("Learn More")} <ArrowUpRight size={16}/></Link></div></motion.article>}
