import {motion} from 'framer-motion';import {useLanguage} from '../../context/LanguageContext';import './PageHero.css';
export default function PageHero({title,subtitle,image,className=''}){const {t}=useLanguage();return <section className={`page-hero ${className}`.trim()}><div className="page-hero-image" style={{backgroundImage:`url(${image})`}}/><div className="container page-hero-content"><motion.div initial={{opacity:0,y:25}} animate={{opacity:1,y:0}} transition={{duration:.6}}><div className="eyebrow">{t('MAUIZA ONLINE ACADEMY')}</div><h1>{t(title)}</h1><p>{t(subtitle)}</p></motion.div><div className="hero-arch">۞</div></div></section>}
