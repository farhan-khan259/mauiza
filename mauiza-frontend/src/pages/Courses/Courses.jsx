import {CheckCircle2} from 'lucide-react';import {motion} from 'framer-motion';import PageHero from '../../components/PageHero/PageHero';import Button from '../../components/Button/Button';import {courses} from '../../data/courses';import {images} from '../../data/images';import {useLanguage} from '../../context/LanguageContext';import './Courses.css';
export default function Courses(){const {t}=useLanguage();return <main className="page"><PageHero title={t("Explore Our Courses")} subtitle={t("Structured Islamic education designed to help you build knowledge, improve your skills and grow in your Quranic journey.")} image={images.hero}/><section className="section courses-intro"><div className="container"><p className="lead">{t("Choose a course that matches your current level and learning goals.")}</p><div className="detailed-courses">{courses.map((course,i)=>{const Icon=course.icon;return <motion.article className={`detailed-course ${i%2?'reverse':''}`} key={course.slug} initial={{opacity:0,y:26}} whileInView={{opacity:1,y:0}} viewport={{once:true}}><div className="detailed-image"><img src={course.image} alt={t(course.title)}/><span><Icon/></span></div><div className="detailed-content"><p className="course-number">{t(`COURSE 0${i+1}`)}</p><h2>{t(course.title)}</h2><h3>{t(course.tagline)}</h3><p>{t(course.description)}</p><h4>{t("What You'll Learn")}</h4><ul>{course.items.map(item=><li key={item}><CheckCircle2/> {t(item)}</li>)}</ul><Button to="/registration" state={{ selectedCourse: course.title }}>{t("Enroll in")} {t(course.title)}</Button></div></motion.article>})}</div></div></section></main>}
